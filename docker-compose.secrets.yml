services:
  proxy:
    secrets:
      - upstream_api_key
      - proxy_api_key

secrets:
  upstream_api_key:
    file: "${UPSTREAM_API_KEY_FILE:-./secrets/upstream_api_key.txt}"
  proxy_api_key:
    file: "${PROXY_API_KEY_FILE:-./secrets/proxy_api_key.txt}"
